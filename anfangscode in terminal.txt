cd C:\Users\Jules\Documents\Master\4.Mastersemester\Masterarbeit\BERT #zu richtigem Ordner auf meinem Computer navigieren

Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope Process #erlauben, Skripte zu laden
.\.venv\Scripts\activate #zu dem Unterordner mit den ganzen BERT-Packages navigieren

#Zwei Skripte laufen lassen:
python 2modernbert_ver2_ohnechunking.py #lädt dependencies/ packages und trainiert Modell fine-tuned für die Texte
python 3embeddings_pca.py #erstellt Embeddings für alle Texte mit fine-tuned Modell und reduziert die Dimensionen mittels PCA auf 168

#Gesamter Prozess muss für die je vier Textteile einzeln durchgeführt werden!
    #Welcher der 4 Textsegmente embedded wird, ist in 2modernbert.py in Zeile 18 einzustellen 
